'use strict'
/*
|--------------------------------------------------------------------------
| Build configuration
|--------------------------------------------------------------------------
|
| This is general purpose file to define configuration for app build.
| You can extend webpack config here
|
*/
module.exports = {
    /*
     ** You can extend webpack config here
     */
    extend(config, ctx) {
    }   
}
